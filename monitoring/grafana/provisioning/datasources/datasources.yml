apiVersion: 1

datasources:
  # Loki — ships Wazuh alerts and ML session logs via promtail
  - name: Loki
    type: loki
    uid: P8E80F9AEF21F6940
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: true

  # Infinity — live polling of historian REST API for physics telemetry
  - name: Historian
    type: yesoreyeram-infinity-datasource
    uid: historian-infinity
    access: proxy
    url: http://host.docker.internal:5000
    editable: true
    jsonData:
      tlsSkipVerify: true
