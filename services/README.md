# Services

Application services used by the honeypot/demo.

- `historian/` — web UI + OPC UA ingest container build context
- `opcua/` — standalone OPC UA server implementation (host-run via `run.sh`)
